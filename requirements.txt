# ViralCut Stage 1 Dependencies

# Core
python-dotenv>=1.0.0
requests>=2.31.0

# Video processing (ffmpeg/ffprobe as system binary: brew install ffmpeg)
Pillow>=10.0.0

# ASR - FasterWhisper
faster-whisper>=1.0.0

# OCR - RapidOCR (primary)
rapidocr-onnxruntime>=1.3.0
onnxruntime>=1.16.0

# OCR - Tesseract (fallback, optional)
# Requires system binary: brew install tesseract
pytesseract>=0.3.10

# OCR - Image hash dedup
imagehash>=4.3.0

# Beat detection
librosa>=0.10.0
soundfile>=0.12.1

# JSON serialization
dataclasses-json>=0.6.0

# =========================================================
# Link pipeline (src/stage1/link_pipeline) dependencies
# =========================================================

# Video download
yt-dlp>=2024.1.0

# Link pipeline ASR (openai-whisper, separate from faster-whisper above)
openai-whisper>=20231117

# Link pipeline OCR (EasyOCR, uses PyTorch)
easyocr>=1.7.0

# Shot detection
scenedetect[opencv]>=0.6.3

# LLM / VLM API (OpenAI-compatible client, used for OpenRouter calls)
openai>=1.30.0

# Link pipeline transitive deps
numpy>=1.24.0
opencv-python>=4.8.0

# =========================================================
# Leo variant Stage 2 (src/stage2/leo_variant) dependencies
# =========================================================

# Structured schemas for the leo_variant multi-agent pipeline
pydantic>=2.0.0
# (also uses `openai` above as the OpenRouter client)